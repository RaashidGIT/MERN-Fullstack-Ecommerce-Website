// Create a form for logged-in users to list an item (name, category, price, image URL, stock count, description).

// Build the backend endpoint POST /api/products that saves the listing tied to the logged-in user's ID (req.user._id).

// Direct impact: Users can immediately contribute inventory to the storefront.